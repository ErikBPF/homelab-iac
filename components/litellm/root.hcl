# LiteLLM provider authentication is native: LITELLM_API_BASE and
# LITELLM_API_KEY are supplied by the operator environment. The later cutover
# wrapper maps the scoped Terraform credential to LITELLM_API_KEY.
#
# Shared state and encryption plumbing is inherited from _shared/root.hcl.
