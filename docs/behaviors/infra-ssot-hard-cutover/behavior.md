# Human behavior seed: infrastructure SSOT hard cutover

> "All iac should be done there. It needs to be a SSOT for our whole infra resources. Check if everything is using terragrunt + tofu and lets check if structure needs to be refactored/improved"

> "lets migrate everything possible to terraform on homelab-iac. Even models. Lets see if renovate/dependabot can help with list there"

> "we can do a hard-cutoff migration and remint credentials. Lets do migration for everything on our sops and .envs. I will do the rest"

> "do recomend. Harden backups and dr palns"

> "go for recomendation. Lets also add sops checks and se what caos engineering we should be using here"

> "create a implementation plan and lets start terra agents to run it. Lets try to follow TDD as much as possible here. Let agents write tests and validate them before executing"
